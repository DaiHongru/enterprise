<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.freework.enterprise.dao.EnterpriseCategoryDao">

    <select id="queryEnterpriseCategoryTop" resultType="com.freework.enterprise.entity.EnterpriseCategory">
        SELECT
        enterprise_category_id, enterprise_category_name, parent_id, create_time, last_edit_time
        FROM
        tb_enterprise_category
        WHERE
        parent_id is null
    </select>

    <select id="queryEnterpriseCategorySecond" resultType="com.freework.enterprise.entity.EnterpriseCategory"
            parameterType="INTEGER">
        SELECT
        enterprise_category_id,
        enterprise_category_name,
        parent_id,
        create_time,
        last_edit_time
        FROM
        tb_enterprise_category
        where
        parent_id=#{parentId}
    </select>

    <select id="queryEnterpriseCategoryById" resultType="com.freework.enterprise.entity.EnterpriseCategory"
            parameterType="INTEGER">
        SELECT
        enterprise_category_id,
        enterprise_category_name,
        parent_id,
        create_time,
        last_edit_time
        FROM
        tb_enterprise_category
        WHERE
        enterprise_category_id=#{enterpriseCategoryId}
    </select>
</mapper>

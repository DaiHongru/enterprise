server:
  port: 9092
eureka:
  instance:
    prefer-ip-address: true
    ip-address: 101.132.152.64
    instance-id: ${eureka.instance.ip-address}:${spring.application.name}:${server.port}
#    ip-address: euaejw.natappfree.cc:80
#    instance-id: ${eureka.instance.ip-address}
  client:
    service-url:
      defaultZone: http://47.102.217.234:8761/eureka/
    eureka-server-connect-timeout-seconds: 30
spring:
  application:
    name: enterprise
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: local
logging:
  level:
    com.freework.enterprise.dao: debug
